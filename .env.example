MYSQL_ROOT_PASSWORD=rootpass
DB_NAME=difol
DB_USER=difol
DB_PASSWORD=difol

# URL que usa el frontend para llamar al backend. Por defecto localhost:4000
# solo funciona si abrís la app desde la misma compu. Para verla desde el
# celular u otro dispositivo en la misma red, poné la IP de tu Wi-Fi, ej:
# VITE_API_URL=http://10.61.12.26:4000
VITE_API_URL=http://localhost:4000
